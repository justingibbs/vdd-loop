<!--
  AGENTS.md — TEMPLATE. Copy into your project root alongside CLAUDE.md.

  This is intentionally a thin pointer, not a second copy. The "how we work"
  guidance lives in CLAUDE.md; keeping it in one place stops the two files from
  drifting apart. Tools that look for AGENTS.md land here and get redirected.

  If your toolchain reads ONLY AGENTS.md and never CLAUDE.md, inline the content
  from CLAUDE.md here instead and make CLAUDE.md the pointer — but keep a single
  source of truth either way.

  Delete these comments once filled in.
-->

# AGENTS.md — [Project Name]

The working guidance for agents in this repo lives in **[CLAUDE.md](./CLAUDE.md)** —
tooling and conventions, the VDD loop, the `SPEC.md` protection rule, and pointers to
`gherkin-guidelines.md` and `evaluation-guidelines.md`.

Read `CLAUDE.md` before working in this project. This file exists only so tools that
look for `AGENTS.md` find their way there; it is deliberately not a second copy.
