<!--
  tasks.md — TEMPLATE (agent-generated and agent-tracked in practice).

  Atomic, implementable units derived from plan.md. A working checklist, not a
  source of truth. Check items off as the loop implements and re-verifies. Each
  task should trace to a requirement (which traces to a verification).

  Delete these comments once filled in.
-->

# tasks.md — [unit-name]

## Tasks

- [ ] **T1** — [atomic, implementable unit]. *(→ R?)*
- [ ] **T2** — [atomic, implementable unit]. *(→ R?)*
- [ ] **T3** — [atomic, implementable unit]. *(→ R?)*

## Notes

- [decisions, additions the loop made — e.g. a task added after a failed
  verification iteration. Reference verification-report.md where relevant.]
- A task that serves no requirement is scope creep — remove it or find the
  missing requirement.
